---
- name: Resolve the selected identity's key store
  ansible.builtin.include_tasks: resolve-key-store.yml

- name: Read key state for the host platform
  ansible.builtin.include_tasks: "read-key-state-{{ ssh_key_platform }}.yml"
