# Homelab TODO

**Created:** 2026-07-09  
**Last updated:** 2026-09-19

- [x] **Restore app-01 Wazuh manager connectivity.** On September 11 I repaired the manager hash-refresh restart and verified app-01 connected after its Purple migration. [Evidence and context](Infrastructure/Compute/Galaxy/Documentation/Change%20Records/app-01%2064%20GiB%20Boot%20Disk%20Replacement%20-%202026-09-11.md).

This file is my central backlog and index. It holds active priorities plus links to system backlogs; implementation steps stay in the owning system's TODO. I keep closed work in [Completed Work](COMPLETED.md).

## Inbox

- [ ] **Observe offline sign-in and unlock on the domain workstations.** I applied the online-sign-in policy on 2026-09-19, verified both clients, and confirmed `HQ-WS001` retained domain membership through a disconnect and recovered its secure channel. Interactive checks remain in the [Active Directory TODO](Platforms/Active%20Directory/Documentation/TODO.md#online-workstation-sign-in-applied-2026-09-19-interactive-checks-open).

- [ ] **Finish the MeshCentral pilot on `docker-blue`.** Deployed 2026-09-12 at https://192.168.40.39, running beside RustDesk. Site administrator claimed and registration closed on 2026-09-13, and `HQ-WS001` is verified reachable on TCP 443 from the guest agent. Agents are installed and connected on `HQ-MGT01`, `DuresaGamingPC`, and `ubuntu-dev` as of 2026-09-13 and on `ObiPC` as of 2026-09-18; `HQ-WS001` is reachable but unenrolled. Moved behind Nginx Proxy Manager at `https://mesh.alphasecunited.com` on 2026-09-13, so agent installs no longer need a patched script. `DuresaGamingPC` was connected after that change and then dropped off; check whether the machine is simply off. `HQ-MGT01` and `DuresaGamingPC` are still installed against `192.168.40.39` and keep working; reinstall them against the name when convenient. Next is testing console access while logged out, Ctrl+Alt+Delete, UAC elevation, and reconnect after reboot. Decide on `localSessionRecording`, which is on and is the only part that grows without bound against 7.3 GiB free. `ObiPC` was enrolled on 2026-09-18 as Background & Interactive and connected within eight seconds, which demonstrates the VLAN 60 path that had only been reasoned from zone membership; run the four tests against it. Retire RustDesk only after the tests pass. [Deployment](Platforms/MeshCentral/Documentation/Change%20Records/Deployment%20-%202026-09-12.md), [registration and path verification](Platforms/MeshCentral/Documentation/Change%20Records/Registration%20Closed%20and%20Test%20Machine%20Path%20Verified%20-%202026-09-13.md).

- [ ] **Complete Action1 Deployer on `HQ-MGT01`.** Whole-domain scope saved, including domain controllers. `HQ-MGT01`, `HQ-WS001`, and `ObiPC` showed Connected on 2026-09-12; `ObiPC` lost its agent in the 2026-09-18 reinstall and the Deployer pushed it back the same evening after I lifted the console exclusion on that machine; decide whether the exclusion goes back on, since it existed to stop the hourly intrusion-prevention alert from the health check. Resolve controller deployment access denied, verify both controllers enroll, and finish local staging cleanup. [Current state and remaining steps](Platforms/Action1/Documentation/Change%20Records/AD%20Deployer%20Preparation%20-%202026-09-12.md).

- [ ] **The `ObiPC` restriction project for `IK-user`, mostly done 2026-09-12.** Enforced that day: two role groups; Chrome and VS Code machine-wide; the Action1 agent; a computer GPO `C-WKS-ObiPC-AppControl` carrying an AppLocker allowlist (Exe/Msi/Appx enforced, Script audit) with a developer carve-out for `C:\Dev` and toolchain paths; a user GPO `U-WKS-ObiPC-Restricted` locking down Settings, the Store, registry tools and Chrome extensions; a SYSTEM scheduled task enforcing an 8 AM-10 PM window and a 240-minute daily budget; and `logonHours` plus `userWorkstations` on the account. Verified on his live session after a forced re-login. Windows was reinstalled on `ObiPC` on 2026-09-18 and the local half rebuilt; the session-limit task was rebuilt with a fix for ticks on an idle machine and then removed by decision the same evening, so the daily budget is gone and only `logonHours` limits when he can sign in; see [ObiPC Rebuild and Rejoin](Platforms/Active%20Directory/Documentation/Change%20Records/ObiPC%20Rebuild%20and%20Rejoin%20-%202026-09-18.md). The reinstall itself turned out to be `IK-user` resetting the machine from the Windows recovery menu, which needs no credentials on Windows 11; the same night I disabled the recovery environment with a SYSTEM task that keeps it off, required an administrator account for every recovery tool, removed the sign-in screen power button and his power menu, replaced the AppLocker policy with one carrying 44 denies for his group and no executable allow of its own, so Action1 is the only install path and a downloaded program cannot run from `C:\Dev` either, blocked executable downloads in Chrome and Edge for him, and reduced Settings, Control Panel and MMC to allowlists. On 2026-09-19 I closed the last install path I had named and left open, the Microsoft Store: three packaged-app denies for his group covering the Store, the Store purchase app and the Xbox app, which installs games without opening the Store. The "Turn off the Store application" policy is ignored on Windows 11 Pro by design, so AppLocker is the only mechanism that works here, see the [incident](Security/Incidents/Active%20Directory/ObiPC%20Wiped%20from%20the%20Recovery%20Menu%20-%202026-09-18.md) and [ObiPC Recovery and Settings Lockdown](Platforms/Active%20Directory/Documentation/Change%20Records/ObiPC%20Recovery%20and%20Settings%20Lockdown%20-%202026-09-18.md). Remaining: observe the user-side policies on his next sign-in; BitLocker with TPM+PIN, which is what would have made the recovery menu harmless; deploy Git, Node.js and Python from Action1; decide the OneDrive per-user path exception; review the Script audit log before enforcing that collection. [ObiPC Restricted User Setup](Platforms/Active%20Directory/Documentation/Change%20Records/ObiPC%20Restricted%20User%20Setup%20-%202026-09-12.md).
- [ ] **Finish the App Portal for `ObiPC`, server live against Action1 since 2026-09-19.** A self-service catalog so `IK-user` can install approved software without asking me. Source is the separate public repository [Duresa7/app-portal](https://github.com/Duresa7/app-portal); the deployment lives at [Platforms/App Portal](Platforms/App%20Portal/README.md). The server runs healthy on `docker-main` at `192.168.40.35:3004` with the API credential rendered from the vault, and all five catalog packages resolve against the tenant. Release v0.2.0 of the Windows client updates itself from GitHub through a `SYSTEM` scheduled task, so a machine is installed once. `HQ-WS001` is enrolled as the test device and reaches the server through a narrow firewall allow added 2026-09-19, and on that evening `testuser`, a standard user with no admin rights, installed 7-Zip from the client in thirty seconds. Remaining: replace that allow with an Nginx Proxy Manager host over TLS and remove it; put the listener behind Nginx Proxy Manager, since a device token is a bearer secret and this is plain HTTP today; register `ObiPC` with `device add` against its current Action1 endpoint (the console lists a stale second entry from before the rebuild) and vault the token; deploy the client through Action1 and confirm `C:\Program Files\App Portal\AppPortal.exe` tests `Allowed` for his group and that the updater task registers and runs; sign the releases so the updater trusts a key rather than the GitHub account. Action1's own Self-Service App Portal is still "Upcoming release" on its roadmap; if it ships first, compare before going further. Records: [deployment](Platforms/App%20Portal/Documentation/Change%20Records/Server%20Deployment%20on%20docker-main%20-%202026-09-19.md), [credential and self-update](Platforms/App%20Portal/Documentation/Change%20Records/Credential%2C%20Catalog%20Verification%20and%20Self-Update%20-%202026-09-19.md).

- [ ] **Decide what to do with Coolify's `apps-02` server on the `Member-1-access` team.** Coolify on `app-01` holds a second server record, `apps-02` at `192.168.80.11`, connecting as `root` with the `apps-01-key`. It belongs to team 1, `Member-1-access`, not the Root Team, which is why the dashboard does not show it unless you switch teams. It is flagged unreachable and nothing at that address answers; no host by that name exists in Galaxy. If the team and its server are finished with, delete the server and the key from that team; if not, the record is waiting for a host that does not exist yet. Found on 2026-09-07 while moving Coolify off root. The `dkadi@wp-01` private key the old item described does not exist in the `private_keys` table. [Non-Root Server Account and Root SSH Disabled](Platforms/Coolify/Documentation/Change%20Records/Non-Root%20Server%20Account%20and%20Root%20SSH%20Disabled%20-%202026-09-07.md).

- [ ] **Decide how far Threat Management coverage goes.** DMZ joined the Selected Networks list on 2026-08-30, and Detection Mode moved to Notify and Block on 2026-08-31, so a signature match on any of the seven inspected networks is now dropped. What is left is coverage: eight of the fifteen routed LANs are still outside inspection: Management, Server-Provision, Proton-WiFi, Cluster-Net, Security-A, MONITOR-A, SERVERS-A and Access-A. That is a throughput trade rather than a switch to flip. [DMZ Added to Threat Management](Infrastructure/Network/UniFi/Documentation/Change%20Records/DMZ%20Added%20to%20Threat%20Management%20-%202026-08-30.md), [Detection Mode to Notify and Block](Infrastructure/Network/UniFi/Documentation/Change%20Records/Detection%20Mode%20to%20Notify%20and%20Block%20-%202026-08-31.md).

- [ ] **Work out how to fill a login in the T3 Code preview browser without the password appearing in any captured output.** On 2026-09-06 that gap stopped me signing in to the Executor console from the preview, and the fallback was to sign in by hand. Options to evaluate: a browser extension inside the preview browser, a bridge that drives the browser from the shell so the value never crosses into captured output, or a session-cookie handoff, which only moves the exposure from the password to the cookie. [Executor](Platforms/Executor/README.md).

- [ ] **Decide whether to ask Executor upstream for a configurable tool call timeout.** Every integration tool call from `execute` dies at 60 seconds, because Executor 1.6.8 calls `callTool` with no options and inherits the MCP SDK default. Nothing on my side can lift it, and patching the image would not survive the next `latest` pull. The fix is upstream: an `EXECUTOR_MCP_TOOL_TIMEOUT_MS` beside the existing `EXECUTOR_SANDBOX_TIMEOUT_MS`, or `resetTimeoutOnProgress` for servers that report progress. Raising it means posting on a public tracker, so it is your call; meanwhile long remote work is detached and polled. [Integration Tool Calls Time Out at 60 Seconds](Platforms/Executor/Documentation/Troubleshooting/Integration%20Tool%20Calls%20Time%20Out%20at%2060%20Seconds%20-%202026-09-07.md).

## Active Priorities

- [x] **Fleet access model, closed 2026-09-07.** The sudo and SSH model that inverted twice in August is the live state on all eleven guests and the standard describes it. The step-by-step history that lived here is in [COMPLETED](COMPLETED.md) and in the maintenance records it links, ending with [Fleet Access Model Verified and Credential Item Collapsed](Operations/Maintenance/Fleet%20Access%20Model%20Verified%20and%20Credential%20Item%20Collapsed%20-%202026-09-07.md). Coolify on `app-01` moved to its own `coolify` account later the same night, so all eleven guests run `PermitRootLogin no`. [Record](Platforms/Coolify/Documentation/Change%20Records/Non-Root%20Server%20Account%20and%20Root%20SSH%20Disabled%20-%202026-09-07.md).
- [ ] **Low priority.** Review the 21 diagrams under [Assets/Diagrams](Assets/Diagrams) and redraw the ones that no longer match the environment. Two are known: [prometheus.svg](Assets/Diagrams/prometheus.svg) still shows 51 targets against a live 54, and `agent-sandbox.png` illustrates the Agent Sandbox design I dropped on 2026-08-06 without ever building it. I did not audit the other 19. Excalidraw stores coordinates and colours as plain numbers in both the `.excalidraw` JSON and the exported SVG, so searching for a figure like `51` matches geometry as often as label text and proves nothing either way. Each diagram has to be opened. Every diagram is Excalidraw with its source beside the SVG, so a redraw edits the `.excalidraw` file and re-exports.

## Scheduled

None.

## System Backlogs

| Backlog | Open items |
|---|---|
| [Active Directory](Platforms/Active%20Directory/README.md) | Forest `ad.alphasecunited.com` on two Windows Server 2025 controllers since 2026-09-09, with a member server, the `HQ-WS001` test workstation, and since 2026-09-11 the first physical end-user workstation `ObiPC`, both domain joined and Microsoft Entra hybrid joined; `ObiPC` was reinstalled and rejoined to its existing objects on 2026-09-18. Hybrid identity with Microsoft 365 proven end to end 2026-09-10 through Entra Cloud Sync with password hash sync; my own account is directory-mastered by soft match with its roles moved to a cloud-only admin account, and it became a Tier 2 workstation administrator on 2026-09-11, then a domain and server administrator on 2026-09-12 by my decision. Windows Admin Center on `HQ-MGT01` is deployed, with five shared connections, AD/DNS extensions, and browser network access verified and successful browser sign-in confirmed 2026-09-12. All five target queries and elevated administrator sessions now pass over WinRM HTTPS. Next: browser workflow exercises, gateway certificate replacement before 2026-11-10, WinRM certificate renewal before 2027-09-12, and RSAT on `ObiPC`; restore the temporary testing state (`PSO-Admins` back to 20, unique passwords on four accounts); credential-validation failure auditing on both controllers; a BitLocker baseline for physical workstations; and people in these records are published as aliases since 2026-09-11. Steps and completion checks in the [platform TODO](Platforms/Active%20Directory/Documentation/TODO.md) |
| [Action1](Platforms/Action1/README.md) | Cloud endpoint management for `ObiPC`, deployed 2026-09-12. Agent 6.0.664.1 in `C:\Windows\Action1\`, service `A1Agent` running as `LocalSystem`. The console-to-endpoint path is proven: a Chrome deployment started in the console executed on the endpoint at 10:14 AM the same day with result `OK`. The organisation identifier in the agent download URL is withheld: it enrols an endpoint with no login. Open: settle where Action1 and Intune divide before `ObiPC` is ever Intune enrolled, because both deploy software. Software deployment steps for the restriction work live in the Inbox item above, not here |
| [App Portal](Platforms/App%20Portal/README.md) | My own self-service software catalog for Action1-managed PCs, built and deployed 2026-09-19. Server container on `docker-main` at `192.168.40.35:3004`, healthy, talking to the tenant with a live API credential, every catalog package verified, device tokens stored as SHA-256 hashes. The Windows client follows the Windows 11 design language, installs to Program Files so the `ObiPC` AppLocker allowlist already covers it, and from v0.2.0 keeps itself current through a `SYSTEM` scheduled task. `HQ-WS001` is enrolled for testing, where a standard user installed 7-Zip through it on 2026-09-19; `ObiPC` is not yet. Source is public at [Duresa7/app-portal](https://github.com/Duresa7/app-portal) |
| [Microsoft Intune](Platforms/Microsoft%20Intune/README.md) | Device management plane for the `alphasecunited.com` tenant, recorded 2026-09-11. Two managed devices: the iPad through Automated Device Enrollment, and `dkadi-mb-air3` enrolled 2026-09-11 through the Company Portal without being erased, personally owned and Microsoft Entra registered. Apple MDM push certificate and Apple Business Manager token both Active until 7/21/2027. Decided 2026-09-11: no compliance policy, and the false Compliant status both devices report is accepted knowingly, so that column carries no information and is not evidence of anything. The standing consequence is that no Conditional Access policy may require a compliant device, because the tenant has one admin account and no break-glass exclusion. Open: FileVault escrow to match the BitLocker baseline, Platform SSO, and whether the two hybrid-joined Windows workstations should be co-managed. Steps and completion checks in the [platform TODO](Platforms/Microsoft%20Intune/Documentation/TODO.md) |
| [Ansible](Platforms/Ansible/Documentation/TODO.md) | `ssh-key-automation` covers 16 hosts after Game 01’s retirement on 2026-09-12: `green-server` joined the `jedi-pc` and `mac` allowlists, `monitor-01` and `ansible-01` remain in the inventory, and the audit found and fixed `ansible-01`'s empty `dkadi` key store. One open item: the onboarding play should create a missing `.ssh` directory with the right owner. `host-access-baseline` holds accounts and sudo; its `sudoers-rootpw.yml` put `Defaults rootpw` on all eleven guests on 2026-08-20. The 2026-07-29 reboot wait is still unexercised |
| [Galaxy](Infrastructure/Compute/Galaxy/Documentation/TODO.md) | All five nodes resolve `permitrootlogin without-password` since 2026-09-07, when `purple-server` and `blue-server` were aligned. Keys only on all five either way. Completed: `app-01` and `edge-01` run on Purple NVMe; boot, services, monitoring, Wazuh, and source-volume cleanup verified. Open: `galaxy-pxe-join` now holds root on all five nodes where it used to be scoped to `grey-server`, and the mechanism that scoped it is gone |
| [Galaxy PXE](Platforms/Galaxy%20PXE/README.md) | Physical deployment complete; keep the reusable one-use service ready for future Galaxy nodes |
| [Media Stack](Platforms/Media%20Stack/Documentation/TODO.md) | Anime release selection moved to the TRaSH Guides profile on 2026-08-27. Three open: whether to backfill the 16 cutoff-unmet Mushoku Tensei episodes, whether Radarr gets anime handling, and whether Recyclarr keeps the tier lists current. The backup-test, capacity-alert, & update-cadence items stay dropped from 2026-07-25 |
| [Splunk Enterprise](Platforms/Splunk/Enterprise/Documentation/TODO.md) | Rocky host OS logs, Proxmox host logs, an `ips_category` breakdown, a machine and severity filter on the Wazuh dashboard, and a verification script for `wazuh_insights`; nine saved searches post to Discord through the alert bot as of 2026-09-03; UniFi flow collection, dashboards, and CIM normalization completed 2026-08-28, the UniFi OS and Protect exports restored with CIM coverage extended to all three products 2026-08-29, and Wazuh added as a data source with its own app and dashboard 2026-08-30 |
| [Splunk Enterprise Security](Platforms/Splunk/Enterprise%20Security/Documentation/TODO.md) | Risk-based alerting, asset and identity framework, a watched-path alert once FIM phase two lands, & ES role review; Wazuh and UniFi alerting to Discord completed 2026-09-03; CIM mapping and eight correlation searches completed 2026-08-28, Wazuh CIM coverage 2026-08-30 |
| [NetBird](Platforms/Netbird/Documentation/TODO.md) | No open items after the 2026-07-12 descope |
| [Nginx Proxy Manager](Platforms/Nginx%20Proxy%20Manager/Documentation/TODO.md) | No open items; Open WebUI became the 24th proxy host on 2026-09-05 |
| [CLI Proxy API](Platforms/CLI%20Proxy%20API/Documentation/TODO.md) | No open items; provider state and the authenticated model list are verified, and the deployment moved to `docker-main` on 2026-08-19 |
| [Prometheus](Platforms/Prometheus/Documentation/TODO.md) | Twenty-four Grafana alert rules deliver to `#bots` through the Discord alert bot, coloured by class, with update alerts announced once and resolved once; 57 targets, after What's Up Docker joined on 2026-09-02, the Open WebUI probe on 2026-09-05, Game 01's retirement on 2026-09-12, the uptime expansion on 2026-09-15 and Portainer's retirement on 2026-09-16. The inert Grafana WAL setting is gone from the host. UniFi gateway metrics. Prometheus uses `restart: always` after Docker's manual-stop flag caused `unless-stopped` to skip the 2026-08-10 boot |
| [Wazuh](Platforms/Wazuh/Documentation/TODO.md) | Alerts forwarded to Splunk, file-integrity monitoring widened, and malware detection added 2026-08-30. Phase two of FIM covers the other 14 agents. Four Wazuh searches alert through Splunk to Discord as of 2026-09-03; no active response, by decision. Central stack upgraded to 4.14.7 on 2026-08-04. Agent `019` (`db-13-dev`/`debian-dev`) deregistered 2026-08-14 via `manage_agents`; release the twelve agent holds one host at a time, then move `edge-01` off 4.14.5. `docker-main`'s 4.14.0 agent turned out on 2026-09-06 to be pointed at the pre-migration manager address and had never connected; I re-enrolled it as `021` on 4.14.6 the same day, so the fleet is 16 active remote agents. The clock deviation is fixed. |
