# Homelab TODO

**Created:** 2026-07-09  
**Last updated:** 2026-09-11

- [x] **Restore app-01 Wazuh manager connectivity.** On September 11 I repaired the manager hash-refresh restart and verified app-01 connected after its Purple migration. [Evidence and context](Infrastructure/Compute/Galaxy/Documentation/Change%20Records/app-01%2064%20GiB%20Boot%20Disk%20Replacement%20-%202026-09-11.md).

This file is my central backlog and index. It holds active priorities plus links to system backlogs; implementation steps stay in the owning system's TODO. I keep closed work in [Completed Work](COMPLETED.md).

## Inbox

- [ ] **Decide what to do with Coolify's `apps-02` server on the `Member-1-access` team.** Coolify on `app-01` holds a second server record, `apps-02` at `192.168.80.11`, connecting as `root` with the `apps-01-key`. It belongs to team 1, `Member-1-access`, not the Root Team, which is why the dashboard does not show it unless you switch teams. It is flagged unreachable and nothing at that address answers; no host by that name exists in Galaxy. If the team and its server are finished with, delete the server and the key from that team; if not, the record is waiting for a host that does not exist yet. Found on 2026-09-07 while moving Coolify off root. The `dkadi@wp-01` private key the old item described does not exist in the `private_keys` table. [Non-Root Server Account and Root SSH Disabled](Platforms/Coolify/Documentation/Change%20Records/Non-Root%20Server%20Account%20and%20Root%20SSH%20Disabled%20-%202026-09-07.md).

- [ ] **Decide how far Threat Management coverage goes.** DMZ joined the Selected Networks list on 2026-08-30, and Detection Mode moved to Notify and Block on 2026-08-31, so a signature match on any of the seven inspected networks is now dropped. What is left is coverage: eight of the fifteen routed LANs are still outside inspection: Management, Server-Provision, Proton-WiFi, Cluster-Net, Security-A, MONITOR-A, SERVERS-A and Access-A. That is a throughput trade rather than a switch to flip. [DMZ Added to Threat Management](Infrastructure/Network/UniFi/Documentation/Change%20Records/DMZ%20Added%20to%20Threat%20Management%20-%202026-08-30.md), [Detection Mode to Notify and Block](Infrastructure/Network/UniFi/Documentation/Change%20Records/Detection%20Mode%20to%20Notify%20and%20Block%20-%202026-08-31.md).

- [ ] **Work out how to fill a login in the T3 Code preview browser without the password appearing in any captured output.** On 2026-09-06 that gap stopped me signing in to the Executor console from the preview, and the fallback was to sign in by hand. Options to evaluate: a browser extension inside the preview browser, a bridge that drives the browser from the shell so the value never crosses into captured output, or a session-cookie handoff, which only moves the exposure from the password to the cookie. [Executor](Platforms/Executor/README.md).

- [ ] **Decide whether to ask Executor upstream for a configurable tool call timeout.** Every integration tool call from `execute` dies at 60 seconds, because Executor 1.6.8 calls `callTool` with no options and inherits the MCP SDK default. Nothing on my side can lift it, and patching the image would not survive the next `latest` pull. The fix is upstream: an `EXECUTOR_MCP_TOOL_TIMEOUT_MS` beside the existing `EXECUTOR_SANDBOX_TIMEOUT_MS`, or `resetTimeoutOnProgress` for servers that report progress. Raising it means posting on a public tracker, so it is your call; meanwhile long remote work is detached and polled. [Integration Tool Calls Time Out at 60 Seconds](Platforms/Executor/Documentation/Troubleshooting/Integration%20Tool%20Calls%20Time%20Out%20at%2060%20Seconds%20-%202026-09-07.md).

## Active Priorities

- [x] **Fleet access model, closed 2026-09-07.** The sudo and SSH model that inverted twice in August is the live state on all eleven guests and the standard describes it. The step-by-step history that lived here is in [COMPLETED](COMPLETED.md) and in the maintenance records it links, ending with [Fleet Access Model Verified and Credential Item Collapsed](Operations/Maintenance/Fleet%20Access%20Model%20Verified%20and%20Credential%20Item%20Collapsed%20-%202026-09-07.md). Coolify on `app-01` moved to its own `coolify` account later the same night, so all eleven guests run `PermitRootLogin no`. [Record](Platforms/Coolify/Documentation/Change%20Records/Non-Root%20Server%20Account%20and%20Root%20SSH%20Disabled%20-%202026-09-07.md).
- [ ] **Low priority.** Review the 21 diagrams under [Assets/Diagrams](Assets/Diagrams) and redraw the ones that no longer match the environment. Two are known: [prometheus.svg](Assets/Diagrams/prometheus.svg) still shows 51 targets against a live 56, and `agent-sandbox.png` illustrates the Agent Sandbox design I dropped on 2026-08-06 without ever building it. I did not audit the other 19. Excalidraw stores coordinates and colours as plain numbers in both the `.excalidraw` JSON and the exported SVG, so searching for a figure like `51` matches geometry as often as label text and proves nothing either way. Each diagram has to be opened. Every diagram is Excalidraw with its source beside the SVG, so a redraw edits the `.excalidraw` file and re-exports.

## Scheduled

None.

## System Backlogs

| Backlog | Open items |
|---|---|
| [Active Directory](Platforms/Active%20Directory/README.md) | Forest `ad.alphasecunited.com` on two Windows Server 2025 controllers since 2026-09-09, with a member server, the `HQ-WS001` test workstation, and since 2026-09-11 the first physical end-user workstation `ObiPC`, both domain joined and Microsoft Entra hybrid joined. Hybrid identity with Microsoft 365 proven end to end 2026-09-10 through Entra Cloud Sync with password hash sync; my own account is directory-mastered by soft match with its roles moved to a cloud-only admin account, and it is a Tier 2 workstation administrator since 2026-09-11. Next: Windows Admin Center on `HQ-MGT01` and RSAT on `ObiPC` as the management console, decided 2026-09-11; restore the temporary testing state (`PSO-Admins` back to 20, unique passwords on four accounts); credential-validation failure auditing on both controllers; a BitLocker baseline for physical workstations; and people in these records are published as aliases since 2026-09-11. Steps and completion checks in the [platform TODO](Platforms/Active%20Directory/Documentation/TODO.md) |
| [Ansible](Platforms/Ansible/Documentation/TODO.md) | `ssh-key-automation` covers 17 hosts since 2026-09-07: `green-server` joined the `jedi-pc` and `mac` allowlists, `monitor-01`, `game-01` and `ansible-01` joined the inventory, and the audit found and fixed `ansible-01`'s empty `dkadi` key store. One open item: the onboarding play should create a missing `.ssh` directory with the right owner. `host-access-baseline` holds accounts and sudo; its `sudoers-rootpw.yml` put `Defaults rootpw` on all eleven guests on 2026-08-20. The 2026-07-29 reboot wait is still unexercised |
| [Galaxy](Infrastructure/Compute/Galaxy/Documentation/TODO.md) | All five nodes resolve `permitrootlogin without-password` since 2026-09-07, when `purple-server` and `blue-server` were aligned. Keys only on all five either way. Completed: `app-01` and `edge-01` run on Purple NVMe; boot, services, monitoring, Wazuh, and source-volume cleanup verified. Open: `galaxy-pxe-join` now holds root on all five nodes where it used to be scoped to `grey-server`, and the mechanism that scoped it is gone |
| [Galaxy PXE](Platforms/Galaxy%20PXE/README.md) | Physical deployment complete; keep the reusable one-use service ready for future Galaxy nodes |
| [Media Stack](Platforms/Media%20Stack/Documentation/TODO.md) | Anime release selection moved to the TRaSH Guides profile on 2026-08-27. Three open: whether to backfill the 16 cutoff-unmet Mushoku Tensei episodes, whether Radarr gets anime handling, and whether Recyclarr keeps the tier lists current. The backup-test, capacity-alert, & update-cadence items stay dropped from 2026-07-25 |
| [Splunk Enterprise](Platforms/Splunk/Enterprise/Documentation/TODO.md) | Rocky host OS logs, Proxmox host logs, an `ips_category` breakdown, a machine and severity filter on the Wazuh dashboard, and a verification script for `wazuh_insights`; nine saved searches post to Discord through the alert bot as of 2026-09-03; UniFi flow collection, dashboards, and CIM normalization completed 2026-08-28, the UniFi OS and Protect exports restored with CIM coverage extended to all three products 2026-08-29, and Wazuh added as a data source with its own app and dashboard 2026-08-30 |
| [Splunk Enterprise Security](Platforms/Splunk/Enterprise%20Security/Documentation/TODO.md) | Risk-based alerting, asset and identity framework, a watched-path alert once FIM phase two lands, & ES role review; Wazuh and UniFi alerting to Discord completed 2026-09-03; CIM mapping and eight correlation searches completed 2026-08-28, Wazuh CIM coverage 2026-08-30 |
| [NetBird](Platforms/Netbird/Documentation/TODO.md) | No open items after the 2026-07-12 descope |
| [Nginx Proxy Manager](Platforms/Nginx%20Proxy%20Manager/Documentation/TODO.md) | No open items; Open WebUI became the 24th proxy host on 2026-09-05 |
| [CLI Proxy API](Platforms/CLI%20Proxy%20API/Documentation/TODO.md) | No open items; provider state and the authenticated model list are verified, and the deployment moved to `docker-main` on 2026-08-19 |
| [Prometheus](Platforms/Prometheus/Documentation/TODO.md) | Twenty-four Grafana alert rules deliver to `#bots` through the Discord alert bot, coloured by class, with update alerts announced once and resolved once; 57 targets after What's Up Docker joined on 2026-09-02 and the Open WebUI probe on 2026-09-05. The inert Grafana WAL setting is gone from the host. UniFi gateway metrics. Prometheus uses `restart: always` after Docker's manual-stop flag caused `unless-stopped` to skip the 2026-08-10 boot |
| [Wazuh](Platforms/Wazuh/Documentation/TODO.md) | Alerts forwarded to Splunk, file-integrity monitoring widened, and malware detection added 2026-08-30. Phase two of FIM covers the other 14 agents. Four Wazuh searches alert through Splunk to Discord as of 2026-09-03; no active response, by decision. Central stack upgraded to 4.14.7 on 2026-08-04. Agent `019` (`db-13-dev`/`debian-dev`) deregistered 2026-08-14 via `manage_agents`; release the twelve agent holds one host at a time, then move `edge-01` off 4.14.5. `docker-main`'s 4.14.0 agent turned out on 2026-09-06 to be pointed at the pre-migration manager address and had never connected; I re-enrolled it as `021` on 4.14.6 the same day, so the fleet is 16 active remote agents. The clock deviation is fixed. |
