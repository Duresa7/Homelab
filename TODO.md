# Homelab TODO

**Created:** 2026-07-09  
**Last updated:** 2026-09-05

This file is my central backlog and index. It holds active priorities plus links to system backlogs; implementation steps stay in the owning system's TODO. I keep closed work in [Completed Work](COMPLETED.md).

## Inbox

- [ ] **Move Coolify off root SSH on `app-01`, and clear out its leftover keys.** Coolify 4.3.17 manages the host it runs on by connecting to it over SSH as `root`, which is why `app-01` logged 735 root logins in the 30 days to 2026-08-15 against 70 for `dkadi`. That blocks the rule that a VM carries no root account, so `app-01` stays an exception until this is done. Read Coolify's own guidance on running against a non-root user before changing anything. Two loose ends belong to the same job: `/root/.ssh/authorized_keys` holds `SHA256:rmcpwm…` with no comment, written 2026-03-13 at 16:37, twenty-six minutes after `/data/coolify` was created, and not one of the four keys Coolify holds today, so it is probably an install-time key that was rotated and never removed. Coolify also stores a private key commented `dkadi@wp-01`, and no host named `wp-01` exists anywhere in the environment.

- [ ] **Put `UPS-01` back on a data cable, and decide whether it stays monitored.** The restart loop is stopped as of 2026-08-31 and the noise is gone, so what is left here is the physical job and the choice behind it. `UPS-01`'s USB cable came off `red-server` on 2026-08-28 when I moved that node onto `UPS-02`, and it is plugged into nothing. The unit still powers the workstation, so it is a live UPS that reports nothing and will not tell anyone it went to battery. Restoring it means plugging the cable into `red-server`, uncommenting the `[ups01]` stanza in `/etc/nut/ups.conf`, `systemctl enable --now nut-server`, restarting `nut-driver-enumerator.service`, and re-enabling the target in both the Prometheus `nut` job and PeaNUT's `settings.yml`. The alternative is to accept it as unmonitored and say so once rather than leave it implied. The finding worth keeping either way: masking `nut-driver@ups01.service` does not stop the loop, because `nut-driver-enumerator` rebuilds the driver under an MD5-hashed instance name and walks straight past the mask. Only removing the device from `ups.conf` works. [ups01 NUT Driver Restart Loop After the UPS Swap](Platforms/PeaNUT/Documentation/Troubleshooting/ups01%20NUT%20Driver%20Restart%20Loop%20After%20the%20UPS%20Swap%20-%202026-08-31.md), [PeaNUT](Platforms/PeaNUT/), [Power](Infrastructure/Hardware/Power.md).

- [ ] **Decide how far Threat Management coverage goes.** DMZ joined the Selected Networks list on 2026-08-30, and Detection Mode moved to Notify and Block on 2026-08-31, so a signature match on any of the seven inspected networks is now dropped. What is left is coverage: eight of the fifteen routed LANs are still outside inspection: Management, Server-Provision, Proton-WiFi, Cluster-Net, Security-A, MONITOR-A, SERVERS-A and Access-A. That is a throughput trade rather than a switch to flip. [DMZ Added to Threat Management](Infrastructure/Network/UniFi/Documentation/Change%20Records/DMZ%20Added%20to%20Threat%20Management%20-%202026-08-30.md), [Detection Mode to Notify and Block](Infrastructure/Network/UniFi/Documentation/Change%20Records/Detection%20Mode%20to%20Notify%20and%20Block%20-%202026-08-31.md).

- [ ] **Close the three account findings the password standardization turned up.** Every `dkadi` and `<REDACTED_PERSONAL_EMAIL>` web login is on the `Account dkadi` password as of 2026-09-04, and the audit behind that change surfaced three accounts that were left alone on purpose and still need a decision. Jellyfin's `Ilyas` has a null `Password` column, so it authenticates with nothing from the user picker; it holds no administrator rights. Portainer's `dashboard` account carries `Role` 1, the same authority as `dkadi`, and looks like an API account for the Homelab Dashboard that should hold a scoped role instead. The Wazuh indexer still defines the shipped demo users `logstash`, `snapshotrestore`, `kibanaro`, `readall` and `anomalyadmin`, none of them reserved, and their default passwords are published upstream. The account credential itself is now centralized in a single `Account dkadi` item rather than one item per service. [Service Login Password Standardization](Operations/Maintenance/Service%20Login%20Password%20Standardization%20-%202026-09-04.md).

## Active Priorities

- [ ] **Fleet access model. This priority inverted twice and the second inversion is the live one.** On 2026-08-14 I gave up on removing `dkadi`'s passwordless sudo, because a live audit showed my keys reach root on the Proxmox nodes and on `ansible-01` regardless, so the gate bought nothing. On **2026-08-15 I reversed that again and went further**: `dkadi` and `ai-agent` are both password-gated now, and a sudo prompt asks for a password that is not the login password. That last part is not a stock behaviour. It needs `Defaults rootpw`, which makes sudo check **root's** password, so root must carry a known password on every guest first, and a locked root password plus that file locks every account out of sudo, recoverable only from the Proxmox console.

  Two passwords, one job each: `standard password` is how `dkadi`, `ai-agent` and `ansible` log in, and `password for root account` is root's password and therefore what every sudo prompt asks for. `ansible` keeps `NOPASSWD` throughout, because it is unattended and gating it would only move the credential into Semaphore. That makes it the route back in if anything below goes wrong.
  - [x] Key hygiene landed 2026-08-15. The Proxmox cluster root key file went from 29 lines to 9, dropping an unidentified key, a `root@Kadi` key and a one-off transfer key that still held root on all five hypervisors; the same unidentified key came off eight guest accounts. `ubuntu-dev` is now a registered identity in `ssh-key-automation`.
  - [x] Repaired what that pass got wrong, 2026-08-15. Two claims in it were false. The `root@Kadi` key was `grey-server`'s own node key under the hostname it had before the rename, and removing it left Grey unable to reach any peer as root, which is also what broke the web interface **Shell** button for every node other than the one serving the page. The sweep that reported zero copies of the unidentified key checked the cluster file and the guest accounts, but Grey's `/root/.ssh/authorized_keys` was a standalone file rather than the symlink the other four nodes carry, so it read none of the cleanup and still authorised that key. Grey's key is reinstated as `root@grey-server`, its file is now the symlink, and all five nodes read one 10-key file. [Broken Node Shell and Standalone authorized_keys on grey-server](Infrastructure/Compute/Galaxy/Documentation/Troubleshooting/Broken%20Node%20Shell%20and%20Standalone%20authorized_keys%20on%20grey-server%20-%202026-08-15.md). The last known surviving copy disappeared with the final retired guest deletion on 2026-08-19.
  - [x] `ai-agent` now exists and accepts a key login on all retained target hosts, provisioned by the new [host-access-baseline](Platforms/Ansible/Source/host-access-baseline/README.md) Ansible project, 2026-08-15. It has no sudo anywhere except `game-01`, through a grant that predates the work. The one finding worth remembering: `media-01` is the only host in the environment with an sshd `AllowUsers` line, and it refused the account while the play reported a clean run. [ai-agent Account Provisioning](Operations/Maintenance/ai-agent%20Account%20Provisioning%20-%202026-08-15.md).
  - [x] The five Proxmox nodes now share one known root password, set 2026-08-15 and proven by a `root@pam` login on each node. They sit outside the guest credential model on purpose. [Node Root Password Reset](Infrastructure/Compute/Galaxy/Documentation/Change%20Records/Node%20Root%20Password%20Reset%20-%202026-08-15.md).
  - [x] The three drifted key comments are normalized, 2026-08-15. My PC's key carried a comment naming the sudo policy this work is reversing, on `edge-01`'s root account and on both of `docker-blue`'s. Each now ends in the canonical identity, with the key data untouched and the key count per file unchanged. A rescan of every readable scope in the environment finds zero copies of the old comment. [SSH Key Comment Normalization](Operations/Maintenance/SSH%20Key%20Comment%20Normalization%20-%202026-08-15.md).
  - [x] `root` and `dkadi` received known passwords on the twelve guests that existed on 2026-08-15. Eleven of those guests remain after the 2026-08-19 retirement. Eight of the original twelve had root locked, which is the condition that makes `Defaults rootpw` lock every account out of sudo on a host, so this had to land before the sudo prompt changes. Both passwords were proven to authenticate rather than only proven to have landed. [Root and dkadi Passwords Set on Twelve Guests](Operations/Maintenance/Root%20and%20dkadi%20Passwords%20Set%20on%20Twelve%20Guests%20-%202026-08-15.md).
  - [x] Root SSH is off on `ansible-01` and `security-01`, 2026-08-15. Both ran `without-password` with zero keys in `/root/.ssh/authorized_keys`, so the setting granted nothing and closing it broke nothing. `app-01` is the third host that ran `without-password` and stays excluded until Coolify moves off root, which is the inbox item above. A `systemctl reload ssh` on `ansible-01` killed the daemon with `fatal: Cannot bind any address`, because `ssh.socket` owns port 22 and a re-execing sshd cannot rebind it; the fix is `restart` rather than `reload` on any socket-activated host. [Root SSH Disabled on ansible-01 and security-01](Operations/Maintenance/Root%20SSH%20Disabled%20on%20ansible-01%20and%20security-01%20-%202026-08-15.md).
  - [x] Decided 2026-08-15: `ansible-01` gets a `dkadi` account, and it now has one with a home directory, `/bin/bash` and `sudo` group membership. The 2026-08-14 objection assumed the account would carry `NOPASSWD`. Under the model chosen on 2026-08-15 it is password-gated with no sudoers drop-in, so group membership is the whole policy and the objection no longer applies.
  - [x] `/etc/sudoers.d/00-rootpw` holding `Defaults rootpw` is on all eleven retained guests, 2026-08-20, `0440 root:root`, each written through `visudo -cf` at a temp path and moved into place only on exit `0`. One host at a time, proving root's password **authenticates** on that host before the file was written to it. A sudo prompt now asks for root's password and refuses the login password, proven both ways on the seven hosts where `dkadi` authenticates; on the four that still hold a `NOPASSWD` drop-in the negative proof is recorded as untestable rather than as a pass, because `NOPASSWD` authenticates nothing. `sudo -l` authenticates too once `rootpw` is in force, so it is no longer a way to check where you stand without a password. Use `sudo -l -U <user>` as root. [Sudo Pointed at the Root Password on Eleven Guests](Operations/Maintenance/Sudo%20Pointed%20at%20the%20Root%20Password%20on%20Eleven%20Guests%20-%202026-08-20.md).
  - [x] The SSH Manager can answer a sudo prompt, 2026-08-20. Ten entries in its env file, one for each guest it connects to as `dkadi`, each holding root's password because `Defaults rootpw` is what every prompt on those hosts now checks. `ssh_execute_sudo` returns root on 11 of 11 guests, including all six where `dkadi` holds no drop-in and five of which I watched the same call fail an hour earlier, and `ssh_execute` without sudo still works on 11 of 11. `ansible-01` has no entry: the SSH Manager connects there as `ansible`, which keeps `NOPASSWD` and never sees a prompt. Two findings worth carrying forward. The file the running server reads is `~/.claude/ssh-manager.env` through `SSH_ENV_PATH`, not the `~/.ssh-manager/.env` the plan named. And configuring a password for a host whose account is `NOPASSWD` hands that password to the command's stdin, because sudo reads stdin only when it prompts, which is live on the four drop-in hosts until the next item removes them. [SSH Manager Sudo Password on Ten Guests](Operations/Maintenance/SSH%20Manager%20Sudo%20Password%20on%20Ten%20Guests%20-%202026-08-20.md).
  - [ ] **Next.** Remove the remaining `NOPASSWD` drop-ins: `dkadi`'s on `docker-network`, `monitor-01`, `game-01` and `media-01`, and `ai-agent`'s on `game-01`.
  - [x] Retired both local SSH Manager environment copies on 2026-08-31. `ubuntu-dev` no longer runs a direct SSH Manager MCP server, so `~/.ssh-manager/.env`, `~/.claude/ssh-manager.env`, the package, and its executable link were isolated during the Executor client cutover. I permanently deleted the credential-bearing rollback archive after the final Claude Alt test passed on 2026-09-01. The stale fallback inventory can no longer become active through a missing `SSH_ENV_PATH`. [Agent client cutover](Platforms/Executor/Documentation/Change%20Records/Agent%20Client%20Cutover%20-%202026-08-31.md).
  - [ ] **Decide whether to rotate the fleet sudo password, and whether the 2026-08-14 exposure gets its own record.** On 2026-08-20 a scan from `$HOME` found root's password, which `Defaults rootpw` now makes the sudo password on all eleven guests, in a session transcript from 2026-08-14, where an `xxd` debug command had printed it and the exposure was flagged but the file was left as it stood. I replaced the value with a `<REDACTED_PASSWORD>` marker on 2026-08-20; the narrative around it is intact and the file still parses. Two things are unresolved. Rotating means changing root's password on all eleven guests and the ten SSH Manager entries together, because `rootpw` ties them. And `Security/Incidents/Grafana/Plaintext Administrator Credential - 2026-07-22.md` is the precedent for a plaintext credential getting an incident record, where this one has none. The narrower lesson is that the first scan only covered `~/.claude_alt/projects/`, because this effort runs under `CLAUDE_CONFIG_DIR`, and missed the default `~/.claude/projects/` entirely. Scan from `$HOME`. [SSH Manager Sudo Password on Ten Guests](Operations/Maintenance/SSH%20Manager%20Sudo%20Password%20on%20Ten%20Guests%20-%202026-08-20.md).
  - [ ] Verify the whole model per host and record the evidence, then rewrite the unpublished Linux host baseline standard to match. Step 7 of that standard, "Lock root", no longer applies to the eleven retained guests, and the "no root on a VM" rule is now "no root **login**". The risk paragraph has to name what I accepted: the sudo password in plaintext on `ubuntu-dev`, and the fact that my keys still reach root fleet-wide through the Proxmox cluster file and through `ansible-01`.
  - [ ] Collapse the credential item to one field per value, deleting the duplicates. **Last**, after the verification, because the tickets before it read fields by name and a delete there is not recoverable.

- [ ] **Do not work this block as written.** It is the pre-2026-08-14 plan, kept for reference until the standard rewrite lands. Its intent, only unattended accounts carry `NOPASSWD` in line with the [Linux host baseline](Guides/Linux-Host-Baseline.md), is back in force after the second inversion, but its steps predate both `Defaults rootpw` and the `ai-agent` account existing, so they describe the right destination by a route I am no longer taking. The live steps are the sub-items under the priority above. The per-host findings and account details stay in the unpublished hardening standard.
  - [ ] Refresh the read-only fleet audit before changing anything. The last full preflight was 2026-08-05, before `ubuntu-dev` and `game-01` reached their current states.
  - [ ] Confirm the exception list. `ubuntu-dev` keeps the approved single-account `ai-agent` model. `game-01` received a human `NOPASSWD` grant on 2026-08-11; either approve and document it as another exception or remove it with the other nonconforming grants.
  - [ ] On each ordinary host that still gives `dkadi` passwordless sudo, provision `ai-agent` first: create the account and home, install only its restricted key, add its validated mode-`0440` `NOPASSWD` drop-in, and add Docker access only where automation needs the socket.
  - [ ] Prove the new automation path through its real SSH key. `sudo -n true` must exit `0`, and approved Compose hosts must also pass `docker info`, before changing human access.
  - [ ] Disable cloud-init on any retained provisioned guest that can restore its old user configuration.
  - [ ] Remove every remaining `dkadi` `NOPASSWD` grant from ordinary hosts while leaving `dkadi` in the `sudo` group. Validate the complete sudoers configuration before ending each host session.
  - [ ] Verify the final state per host: `dkadi` passwordless sudo fails, password-backed sudo succeeds, unattended accounts retain `sudo -n`, SSH remains key-only with root login disabled, and cloud-init cannot recreate a removed grant. Update the private per-host findings and close this priority only when every ordinary host passes.

- [ ] **Low priority.** Review the 21 diagrams under [Assets/Diagrams](Assets/Diagrams) and redraw the ones that no longer match the environment. Two are known: [prometheus.svg](Assets/Diagrams/prometheus.svg) still shows 51 targets against a live 56, and `agent-sandbox.png` illustrates the Agent Sandbox design I dropped on 2026-08-06 without ever building it. I did not audit the other 19. Excalidraw stores coordinates and colours as plain numbers in both the `.excalidraw` JSON and the exported SVG, so searching for a figure like `51` matches geometry as often as label text and proves nothing either way. Each diagram has to be opened. Every diagram is Excalidraw with its source beside the SVG, so a redraw edits the `.excalidraw` file and re-exports.

## Scheduled

None.

## System Backlogs

| Backlog | Open items |
|---|---|
| [Ansible](Platforms/Ansible/Documentation/TODO.md) | `ubuntu-dev` registered and the cluster key file deduplicated, both 2026-08-15. New `host-access-baseline` project holds accounts and sudo; it set the account passwords on the twelve guests that existed then, and its `sudoers-rootpw.yml` play put `Defaults rootpw` on all eleven retained guests on 2026-08-20. The 2026-07-29 reboot wait is still unexercised. `jedi-pc`, `mac` and `ansible-control` need `green-server` on their allowlists, and `monitor-01`, `game-01` and `ansible-01` are absent from the `ssh-key-automation` inventory. `ansible-01` moved from `Etc/UTC` to `America/New_York` on 2026-09-01 |
| [Galaxy](Infrastructure/Compute/Galaxy/Documentation/TODO.md) | `purple-server` and `blue-server` run `permitrootlogin yes` where the other three run `without-password`. Keys only on all five either way. `galaxy-pxe-join` now holds root on all five nodes where it used to be scoped to `grey-server`, and the mechanism that scoped it is gone |
| [Galaxy PXE](Platforms/Galaxy%20PXE/README.md) | Physical deployment complete; keep the reusable one-use service ready for future Galaxy nodes |
| [Media Stack](Platforms/Media%20Stack/Documentation/TODO.md) | Anime release selection moved to the TRaSH Guides profile on 2026-08-27. Three open: whether to backfill the 16 cutoff-unmet Mushoku Tensei episodes, whether Radarr gets anime handling, and whether Recyclarr keeps the tier lists current. The backup-test, capacity-alert, & update-cadence items stay dropped from 2026-07-25 |
| [Splunk Enterprise](Platforms/Splunk/Enterprise/Documentation/TODO.md) | Rocky host OS logs, Proxmox host logs, an `ips_category` breakdown, a machine and severity filter on the Wazuh dashboard, and a verification script for `wazuh_insights`; nine saved searches post to Discord through the alert bot as of 2026-09-03; UniFi flow collection, dashboards, and CIM normalization completed 2026-08-28, the UniFi OS and Protect exports restored with CIM coverage extended to all three products 2026-08-29, and Wazuh added as a data source with its own app and dashboard 2026-08-30 |
| [Splunk Enterprise Security](Platforms/Splunk/Enterprise%20Security/Documentation/TODO.md) | Risk-based alerting, asset and identity framework, a watched-path alert once FIM phase two lands, & ES role review; Wazuh and UniFi alerting to Discord completed 2026-09-03; CIM mapping and eight correlation searches completed 2026-08-28, Wazuh CIM coverage 2026-08-30 |
| [NetBird](Platforms/Netbird/Documentation/TODO.md) | No open items after the 2026-07-12 descope |
| [Nginx Proxy Manager](Platforms/Nginx%20Proxy%20Manager/Documentation/TODO.md) | No open items; Open WebUI became the 24th proxy host on 2026-09-05 |
| [CLI Proxy API](Platforms/CLI%20Proxy%20API/Documentation/TODO.md) | No open items; provider state and the authenticated model list are verified, and the deployment moved to `docker-main` on 2026-08-19 |
| [Prometheus](Platforms/Prometheus/Documentation/TODO.md) | Twenty-four Grafana alert rules deliver to `#bots` through the Discord alert bot, coloured by class, with update alerts announced once and resolved once; 57 targets after What's Up Docker joined on 2026-09-02 and the Open WebUI probe on 2026-09-05. The inert Grafana WAL setting is gone from the host. UniFi gateway metrics. Prometheus uses `restart: always` after Docker's manual-stop flag caused `unless-stopped` to skip the 2026-08-10 boot |
| [Wazuh](Platforms/Wazuh/Documentation/TODO.md) | Alerts forwarded to Splunk, file-integrity monitoring widened, and malware detection added 2026-08-30. Phase two of FIM covers the other 14 agents. Four Wazuh searches alert through Splunk to Discord as of 2026-09-03; no active response, by decision. Central stack upgraded to 4.14.7 on 2026-08-04. Agent `019` (`db-13-dev`/`debian-dev`) deregistered 2026-08-14 via `manage_agents`; release the twelve agent holds one host at a time, then move `edge-01` off 4.14.5 and `docker-main` off 4.14.0. The clock deviation is fixed. |
